#version 120

uniform sampler2D texture;
varying vec2 v_texCoord;
uniform float blinking;

void main() 
{
    vec4 texCol = texture2D(texture, v_texCoord);        
    if (texCol.x < 0.3)
    {
        texCol.y += 0.2 * (1 + sin(blinking));
    }
    gl_FragColor = texCol;
}